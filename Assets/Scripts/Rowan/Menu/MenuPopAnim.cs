using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MenuPopAnim : MonoBehaviour
{
    void Start()
    {
        if (Application.isPlaying)
        {
            print("In player or playmode");
        }
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
